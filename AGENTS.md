# FormatOwl 工程约定

遵循用户提供的全局规则，尤其是不擅自 push、PR、远程 CI 或部署。

- `apps/web` 负责页面、认证和短请求，不能处理媒体字节或启动 FFmpeg。
- `apps/worker` 负责媒体处理与清理。更改处理逻辑后重启 worker。
- `packages/core` 共享参数、服务与类型。不要为尚未开发的工具建立假处理器。
- 注册登录使用 Better Auth 与现有 PostgreSQL。本地邮件流程已验证；Google OAuth 与 Resend 尚待凭据和域名配置，不能写成真实第三方登录或邮件送达已验收。
- 新的数据库结构使用新迁移文件；服务端校验归属，不接受客户端指定对象 key、编码命令或账单金额。
- 修改上传或任务状态机时运行相关集成与浏览器测试；纯文案 / 样式改动不添加镜像实现的单元测试。
- 运行检查见 README。故障恢复测试必须在普通 worker 已停止时执行，测试仅终止自身创建的进程组。
- `.env`、上传内容、生成媒体和测试浏览器状态不得提交。
