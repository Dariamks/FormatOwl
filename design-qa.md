# 视频剪切与画面裁剪验收

日期：2026-09-15。范围：用户提供的七张截图所指定的两个编辑器；沿用 FormatOwl 品牌、头部导航及既有真实处理流程。

## 对照依据

附件目录：`/var/folders/ns/ds37t0716qd6_y1m1bswswwm0000gn/T/`。

| 参考 | 附件文件                                                   | 核对内容                                           |
| ---- | ---------------------------------------------------------- | -------------------------------------------------- |
| 图 1 | `codex-clipboard-df290dee-35bc-4530-bb6d-8211c2382258.png` | 无原生控制条的画面；独立播放、静音、时间；左右双栏 |
| 图 2 | `codex-clipboard-9c119926-1800-4efd-a5e4-06cbb11a3ea1.png` | 缩略图选区、两端手柄、播放游标与时间               |
| 图 3 | `codex-clipboard-978ce966-1e4c-4522-9c71-c0cd48e55c66.png` | MP4/MKV/MOV 导出菜单                               |
| 图 4 | `codex-clipboard-d0e4f3ee-211e-42d9-839d-48d0704792b6.png` | 保留/移除模式及说明文字                            |
| 图 5 | `codex-clipboard-1571ddc7-198d-4ca9-b835-749af92a8f90.png` | 裁剪网格、八个控制点、播放条和导出格式             |
| 图 6 | `codex-clipboard-0cc0c03c-2d7e-43ae-9663-b698e61ca22b.png` | 改善原页面的播放/暂停按钮和裸进度条                |
| 图 7 | `codex-clipboard-4f4b6fe8-83ca-435d-8e0b-fa3ffcc898f6.png` | 两行四列比例按钮、宽×高精确尺寸、格式选择          |

图 1/3/4/5/6 原始尺寸为 2940×1666；图 2 为 1328×230，图 7 为 816×756。比较时以编辑器卡片或设置面板归一宽度，排除浏览器工具栏、参考网站侧栏与红色批注框；不是整站逐像素克隆。

测试素材为本地真实 `sample.mp4`（960×540、5 秒的彩色测试图案），参考图为猫视频。此处只比较控件、网格、布局和视频显示比例，不把动态素材内容差异当成设计偏差。使用现有 Lucide 图标和品牌资产，没有新增装饰性图片或图片占位。

## 截图与状态

以下路径相对仓库根目录；媒体和截图位于被 Git 忽略的 `.data/` 中。

| 页面/状态                                  | 截图                                    |
| ------------------------------------------ | --------------------------------------- |
| 剪切，中文桌面，单选区、MOV、暂停          | `.data/video-cutter-design-desktop.png` |
| 剪切，导出菜单展开                         | `.data/video-cutter-format-menu.png`    |
| 剪切，390 px 触屏拖动后                    | `.data/video-cutter-design-mobile.png`  |
| 剪切，英文多选区、移除模式、MKV            | `.data/editor-cut-desktop.png`          |
| 裁剪，中文桌面，原比例、MKV、暂停在 2.5 秒 | `.data/video-crop-design-desktop.png`   |
| 裁剪，右侧设置特写                         | `.data/video-crop-settings-detail.png`  |
| 裁剪，768 px 平板                          | `.data/video-crop-design-768.png`       |
| 裁剪，390 px 手机                          | `.data/video-crop-design-390.png`       |
| 裁剪，旋转视频、中文触屏                   | `.data/editor-crop-mobile.png`          |

自动化截图使用 Chromium，桌面视口 1365×900，移动视口 390×844、平板 768×844，设备像素比为 1。最终全页截图从页面顶部拍摄，避免滚动中的粘性头部遮住画面。另使用 Codex 内置浏览器实查中文页面、5:4 比例和菜单；内置浏览器的截图缩放有偏差，正式对照采用自动化截图。

## 视觉及交互检查

- **版式与间距：** 剪切为大画面、独立播放行、缩略图、选区列表；右侧依次为模式说明、文件、时间和导出。裁剪比例为 2×4 网格，宽×高同一行，X/Y 与重置可展开。裁剪设置按内容高度排列，桌面滚动时保持可见，不被下方实时效果预览撑高；900 px 以下改为纵向布局。
- **字体与文案：** 沿用 FormatOwl 中文字体和标题层级，时间使用等宽数字。补充保留/移除的实际处理说明。时间输入为 `HH:MM:SS.mmm`，兼容直接输入秒数。参考图中的机器翻译“移动”显示为 MOV，“原创”显示为原比例。
- **颜色、边框与图标：** 使用 FormatOwl 蓝色表达选中状态；缩略图端点为蓝色手柄与白色竖线；裁剪为白色网格、蓝白圆形控制点，外围变暗。播放条为浅灰底，播放、静音和音量均使用现有图标体系。
- **状态与操作：** 验证加载、暂停/播放、静音恢复、音量归零恢复、连续键盘调节、时间输入错误、格式展开、草稿恢复、选区增删和裁剪重置；画面内不显示原生控件。导出结果页仍提供播放器和下载。
- **可访问性与响应式：** 比例使用原生单选语义，按钮有名称与选中状态，时间轴手柄具有 slider 语义；数字输入与键盘操作提供拖动替代。手机端未出现持续横向溢出，实际触屏拖动通过。中英文界面均检查。

## 修正记录

| 问题                             | 级别 | 修正与复验                                                                 |
| -------------------------------- | ---- | -------------------------------------------------------------------------- |
| 原生视频控制条遮住剪切画面       | P1   | 移除 controls，新增画面外播放行；浏览器确认 controls=false 且可播放        |
| 视频导出固定 MP4                 | P1   | 三种真实封装；MKV/MOV 额外生成 MP4 预览；下载与播放验证通过                |
| 裁剪手柄集中在角落               | P2   | 明确四角与边缘中点定位；鼠标、触屏移动及右下角缩放通过                     |
| 连续调节进度/音量时短暂恢复旧值  | P1   | 输入与静音动作即时同步 UI 状态，再接收媒体事件；连续键盘和静音归零恢复通过 |
| 暂停画面持续重绘、设置卡片过高   | P2   | 暂停时只在编辑/跳转/加载事件重绘；右侧独立高度并支持桌面粘性定位           |
| 尺寸输入附带 px 导致标签匹配含糊 | P2   | 显式提供中英文输入名称；桌面与移动测试通过                                 |

测试用例同时修正了异步数值读取、测试素材尺寸假设、按 DOM 顺序选错裁剪手柄，以及全页截图的滚动位置。桌面缩小为手机视口时，等待 ResizeObserver 完成坐标更新后确认没有横向溢出。

## 验证证据

- `pnpm test`：16 项通过，覆盖新格式允许列表、未知格式拒绝、旧参数兼容及原有边界。
- `pnpm test:editors`：45 项真实输出/准备检查通过，另验证明确拒绝 HDR 画面处理；包含两个视频工具各三种容器、MIME、MOV 品牌标记、MKV 封装及 MP4 预览解码。
- `pnpm test:editor-api`：通过；包含真实 MKV/MOV 下载、预览 MIME、越权拒绝、格式幂等冲突和既有源文件生命周期检查。
- `tests/e2e/media-editors.spec.ts`：5 项通过；四种工具真实播放和下载、音频拼接、移动裁剪与草稿恢复。
- `tests/e2e/video-editing-controls.spec.ts`：2 项通过；播放/暂停、连续进度和音量调节、静音、时间校验、鼠标与触屏手柄、八种比例入口及 5:4 尺寸、格式草稿和三种视口。
- 最终 `pnpm typecheck`、`pnpm build` 通过。worker 在媒体处理代码修改后已重启，保持本地运行。

本次未改上传/任务状态机、迁移、登录或部署；原压缩与 ZIP 的既有验收见 `docs/acceptance-editors.md`，本轮不将它们声称为再次全量验收。

最终视觉与交互复验无未解决的 P0/P1/P2 项。

Final result: passed

## 2026-09-15 补充：剪切设置紧凑布局

本次参考用户新增的两张截图：`codex-clipboard-0a0c3666-89ba-4ea4-9ac7-260d334ce3b7.png`（剪切右侧空白问题）与 `codex-clipboard-909899b2-1a79-44d5-83e0-53478f761817.png`（裁剪页目标布局），原图均位于 `/var/folders/ns/ds37t0716qd6_y1m1bswswwm0000gn/T/`。

- 将剪切与裁剪统一为顶部对齐、设置卡片按内容撑开，预计导出与格式选择之间采用固定间距；消除截图红框标注的大块空白。
- 两个视频工具共用桌面粘性设置面板，900 px 以下恢复普通纵向排列，移除裁剪专用的重复样式分支。
- 重新生成并查看 `.data/video-cutter-design-desktop.png` 与 `.data/video-cutter-design-mobile.png`，与新增裁剪参考图一同对照；确认格式、预计时长和按钮连续排列，手机无横向溢出。
- 本次运行 `tests/e2e/video-editing-controls.spec.ts`：2 项通过，覆盖剪切和裁剪的桌面、触屏及草稿恢复。改动文件格式化通过。
- 本次 `pnpm typecheck` 未通过：`apps/web/src/components/task-detail.tsx:44` 将包含 `transcription` 的工具类型传给不接受该类型的 `BatchCompressor`。这是本次布局改动之外的代码；此轮没有修改该文件，也没有重新运行上方历史记录中的媒体处理或构建检查。

本次布局视觉与交互复验通过；全项目类型检查仍存在上述错误。

## 2026-09-16：转写工作区优化

本轮参考：`/var/folders/ns/ds37t0716qd6_y1m1bswswwm0000gn/T/codex-clipboard-d8c6eb26-7bcf-41c3-80dc-35c029acd2c7.png`。目标是把现有真实转写改为阅读优先的工作区，沿用 FormatOwl 品牌与 AI 阅读能力。

### 对照与截图

- 原图 2940 × 1666；裁去浏览器顶部 170 像素后，按 2× 密度归一为 1470 × 748 的应用区域。标准化参考：`.data/transcript-redesign/reference-normalized.png`。
- 同尺寸复验：在应用内浏览器读取到 CSS 视口 1470 × 748、devicePixelRatio 0.9。浏览器截图提供方输出 1633 × 830 并在右侧和下方附加空白；保留原始文件 `.data/transcript-redesign/reference-size-raw-final.png`，仅为对照将有效左上区域 817 × 416 归一为 1470 × 748。
- 全景并排对照：`.data/transcript-redesign/comparison-final.png`；标题、正文首段和操作栏局部对照：`.data/transcript-redesign/comparison-detail-final.png`。归一化引起的轻微模糊属于截图采样差异。
- 无视口覆盖的真实页面截图：`.data/transcript-redesign/desktop-live-final.png`，900 × 830 像素。桌面状态为真实 55 秒原创合成语音转写完成、真实 AI 摘要完成、播放器暂停。
- 手机截图：`.data/transcript-redesign/mobile-editor.png`，390 × 844、1×，展示 105 段测试内容、后页编辑及 1.5× 播放设置。应用内浏览器另检查手机正文／AI 面板切换、引用定位与横向溢出。

### 修正记录与最终判断

| 项目         | 初次发现                                     | 修正与复验                                                                                                                           |
| ------------ | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 字体与排版   | P2：全局标题负字距导致文件名重叠             | 文件名恢复正常字距、单行省略；正文 15–18 px，摘要区建立标题与段落层级。最终局部对照通过。                                            |
| 颜色与控件   | P2：原有阅读空状态样式使生成按钮文字对比不足 | 转写范围内明确按钮前景和品牌色；空状态可读，生成操作真实完成。                                                                       |
| 空间与布局   | P2：独立状态栏压缩首屏正文空间               | 状态并入文件标题，采用桌面正文／摘要双栏与独立滚动、手机标签切换、固定底部播放栏。最终全景通过。                                     |
| 交互与响应式 | 已覆盖                                       | 按段编辑、复制、播放、前后片段、倍速、静音、自动滚动、导出和手机面板均可用；实际点击摘要 00:43 引用后音频跳到 43 秒并高亮正文。      |
| 图像与图标   | 可接受的产品差异                             | 本页为文字和标准控件，复用项目 Lucide 图标；摘要呈现真实生成的段落、列表和引用。人物演讲内容及参考图的信息卡属于参考产品的样本数据。 |
| 文案与内容   | 可接受的产品差异                             | 中英界面保留；初始说话人显示可读编号，人工改名保留。实际语音内容与参考样本不同，因此段落长度和摘要结构不同。                         |

复制读取所有分页；AI 使用完整的已保存转写快照。引用携带快照时间，后页引用可加载并定位；编辑后明确标识旧摘要。试听地址定时更新保留播放位置与速度。无逐词时间数据，高亮保持段级。

### 本轮验证

- `pnpm test`：47 项通过。
- `pnpm test:transcript-reading`（新增脚本）：全文快照、权限、幂等、引用、Markdown 下载、版本失效、取消、思维导图、问答与过期通过；固定 AI 响应，真实数据库和处理流程。
- `pnpm test:reading`：现有 AI 阅读服务回归通过，包含恢复、引用校验与导出。
- 转换／转写浏览器回归：6 项通过；后续转写定向复测通过，新增 12 分钟试听地址更新后保留位置检查。
- 原翻译阅读布局浏览器回归：4 项通过。
- TypeScript 检查和生产构建通过；应用内浏览器未捕获 error/warn。
- 另用本次原创语音执行实际上传、百炼转写、摘要生成、旧摘要更新与引用回听；实际模型质量仅按该样本核对。
- 本地 web 和 worker 已重启。未新增迁移，未 push、PR、远程 CI 或部署。

实施检查项已完成；无待修复的 P0/P1/P2。P3：长会议在不同说话人和段落长度下仍可继续微调阅读密度。

final result: passed

## 2026-09-16：全站下拉框统一

按用户第四张图中的视频编码下拉框，复用已有 Radix Select 和平台样式，替换 6 个组件中的 19 处原生下拉框：转写入口 2 处、翻译入口 3 处、AI 阅读 3 处、翻译编辑与导出 4 处、媒体编辑 3 处、转写工作区 4 处。

- 菜单统一为白色圆角浮层、灰色选中项和右侧勾选图标；长列表可以滚动，空间不足时自动调整展开方向。
- 保留语言、音轨、音质、倍速、说话人和导出选项的数据类型与业务回调。未标注说话人仍保存为 null；仅剩一位说话人时禁用合并。
- 新增 SelectField 复用平台控件，并把原生 dialog 内的菜单挂载到同一个 dialog，避免浮层位于遮罩后方或无法点击。Esc 先关闭菜单，焦点回到触发器，弹窗保持打开。
- 源码复查没有遗留手写 `<select>` 或 `<option>`；Radix 为表单兼容产生的隐藏元素不作为可见原生控件。

验证：

- `pnpm typecheck`、`pnpm build`、本轮变更文件 Prettier 检查通过。
- 23 项不同浏览器用例最终全部通过：转换与转写 6 项、媒体编辑 5 项、AI 阅读 4 项、翻译 6 项、新增下拉框键盘与手机交互 2 项。
- 首轮两个媒体测试的断言完成后，Playwright 关闭上下文时报告 trace 文件 ENOENT；改用本轮独立输出目录重跑，两项通过。新增测试修正弹窗名称定位，并等待 Radix 键盘焦点更新后验证 Home/End，复测通过。
- 应用内浏览器实测转写语言、翻译目标语言、导出格式。截图位于 `.data/platform-selects/`：`transcription-live.png`、`video-language-live.png`、`export-live.png`；390 × 844 手机截图为三个 `*-translator-mobile.png`。

本轮仅修改前端控件与相关浏览器测试，无依赖、数据库或 worker 处理逻辑变更。

---

# PDF 双语连续阅读布局验收（2026-09-16）

范围：用户本轮图二的页面布局，以及图三／四的底部悬浮分页缩放条与左右同步滚动。仅修改网页阅读层，保留既有 worker 渲染、保存、校正、下载与 AI 服务。

## 对照与环境

- source visual truth path: `/var/folders/ns/ds37t0716qd6_y1m1bswswwm0000gn/T/codex-clipboard-37be6d03-9a61-4dda-952e-e06c2a646b75.png`
- implementation screenshot path: `.data/pdf-reader-qa/desktop-reference-state.png`
- viewport: 1470 × 749 CSS px；Chromium 截图密度 1，输出 1470 × 749 px。
- 参考图原始像素 2940 × 1666；移除顶部 168 px 浏览器工具栏后按 2 倍密度归一为 1470 × 749。归一副本：`.data/pdf-reader-qa/reference-normalized.png`。
- state: 中文界面、双语、100% 缩放、第一页稍向下滚动；悬停右侧文档。参考是英文界面且悬停左栏，两种悬停均单独验证。
- 全图并排比较：`.data/pdf-reader-qa/comparison.png`；顶部控件特写：`.data/pdf-reader-qa/comparison-controls.png`。已实际打开两张比较图检查。
- 验证数据来自用户所示本地 22 页 PDF 的已保存数据和已有预览。浏览器使用内存 API 固定响应，视觉比较中设为完成状态，未更改原任务状态，未重新调用翻译或 AI。文档译文仍由 FormatOwl 的现有排版器生成，与参考网站的字体、译文内容及分页不同，不属于本轮布局复制范围。

## 视觉检查与迭代

1. 默认阅读不再用 AI 占据右半栏；两个独立文档卡片等宽（695 px），左右边距 32 px，栏间距 16 px，卡片顶端 141 px，工具栏高 52 px。连续页面保留原始纵横比。
2. 字体与文案：界面为 14 px 控件、16 px 文件标题，保留中文本地化；正文直接使用实际页面图。原文没有使用截图文字或假图片替代。
3. 颜色与控件：白底、浅灰文档背景、浅灰细边框、蓝色选中胶囊，圆形校正按钮位于右下角。复用既有 Lucide 图标；没有新增图像资产或依赖。
4. P2（已修复）：首次阅读选中的文字框出现蓝色边框。阅读状态清除区域选中显示，点击区域仍进入校正。对照图确认默认文档无选中框。
5. P2（已修复）：AI 面板外移的顶部操作条被工作区 overflow 裁切。PDF 工作区允许操作条显示，文档滚动仍由内部容器约束。修复后截图 `.data/pdf-reader-qa/ai-controls-fixed.png`，并实际点击关闭，确认恢复两栏。
6. 窄屏边界（已修复）：从移动端原文标签切到仅译文时，原有隐藏规则会隐藏译文。规则限定在双语模式，复验通过。
7. 后续截图：`.data/pdf-reader-qa/keyboard-controls.png` 验证键盘聚焦工具条可显示；`.data/pdf-reader-qa/mobile-translation.png` 验证 390 × 844 单栏布局与页间连续滚动。手机文件／AI 标签随后统一为同一蓝色主题。

## 交互验证

通过 agent-browser 运行本地浏览器检查（脚本、固定数据和截图均位于忽略目录 `.data/pdf-reader-qa/`）：

- 两栏等宽、顶部和底部对齐、页面无横向溢出。
- 非悬停时工具条透明且不接收鼠标事件；只在当前悬停栏显示。键盘聚焦时可见，触屏使用 CSS 媒体规则保持可用。
- 左侧向下滚动 1800 px，两侧位置误差小于 2 px；右侧继续滚动 1300 px 同样同步。
- 下一页、直接输入页码、共同放大到 125%、重置到 100%；缩放后继续滚动保持同步。
- 初始只挂载附近页面的译文预览，未同时渲染全部 22 页。
- 仅译文／双语、版式／文字、校正打开和关闭、下载对话框均可用。
- 顶部摘要和问答切换、AI 关闭后恢复双语。仅验证界面和既有缓存，未验收新生成内容。
- 390 px 下无横向溢出；原文／译文标签工作，切换到仅译文后仍有可见文档。
- 最后一次浏览器应用异常列表为空。
- `pnpm --filter @filemorph/web typecheck` 通过；五个相关组件与样式文件的 Prettier 检查通过。

首次下载断言误选到页面另一隐藏 dialog，改为定位“下载翻译”的已打开 dialog 后验证通过；属于检查脚本修正。浏览器 CLI 的 wheel 命令未产生实际滚动，改用其带容器选择器的 scroll 命令完成双向滚动验证。

没有待处理的 P0/P1/P2 布局或交互问题。没有执行提交、push、PR、远程 CI 或部署。

final result: passed

# PDF 仅译文与 AI 阅读布局验收（2026-09-16）

范围：用户补充的“仅译文”参考图。修正仅译文占满整屏的问题，统一左侧译文与右侧 AI 的等宽布局、摘要设置和导图操作条。

## 视觉对照

- source visual truth path: `/var/folders/ns/ds37t0716qd6_y1m1bswswwm0000gn/T/codex-clipboard-cc94d717-55ba-4047-b651-c33271a2a945.png`
- 补充参考：同目录的 `codex-clipboard-0cc0cfc1-f02b-465d-bb5f-b2d33cd51eea.png`（详细程度）、`codex-clipboard-2003ac3f-e622-47ed-8837-773fae34b087.png`（模板）、`codex-clipboard-85d0ef7f-9436-4f59-8cb2-e5e79f44baaf.png`（思维导图）。
- implementation screenshot paths: `.data/pdf-only-qa/desktop-summary.png`、`.data/pdf-only-qa/desktop-mindmap.png`。
- viewport: 1470 × 749 CSS px，截图密度 1。参考去除顶部 168 px 浏览器工具栏后按 2 倍密度归一。
- 已实际打开 `.data/pdf-only-qa/summary-comparison.png` 与 `mindmap-comparison.png` 并排检查。两侧卡片均宽 695 px，间距 16 px，卡片顶部 141 px；语言与下载移到译文卡片右上方。
- 摘要设置、正文间距、蓝色生成按钮和白色导图画布已对齐参考。导图保留现有可折叠节点与出处交互，工具栏收为放大、缩小、适应、下载四项。译文排版和导图节点图形沿用当前渲染器，并非参考网站的生成内容。
- 下拉菜单截图：`.data/pdf-only-qa/depth-menu.png`、`template-menu.png`；保留平台 Radix 选择控件的键盘操作与选中标记。
- 手机端 390 × 844。截图复核发现重新生成文字被固定按钮宽度截断，已修复为带无障碍名称的图标按钮。修复后截图 `.data/pdf-only-qa/mobile-summary-fixed.png`，已打开检查，按钮宽 32 px、图标宽 15 px，无页面横向溢出。

## 行为与验证

- 点击仅译文自动打开右侧 AI；点击双语恢复原文／译文两栏。选中 AI 标签自动使用译文加 AI 布局，再次点击当前标签保持面板打开。校正关闭后恢复之前的布局。
- PDF 摘要默认详细，提供详细／默认／全面；模板为 AI 笔记、摘要、会议纪要、分析、评述。选项已接入共享请求校验与两种 provider 请求构造，不仅是菜单文案。旧文件类型和旧缓存选项保持可读。
- agent-browser 20 项检查通过，结果 `.data/pdf-only-qa/browser-checks.json`；包含等宽布局、菜单选项及请求字段、问答切换、导图缩放与适应、双语同步滚动、悬停分页、校正恢复、手机切换和桌面尺寸恢复。首次检查的双语面板断言使用了不存在的 class；改用实际 `.pdf-reader-pane` 后通过，属于检查脚本修正。
- `pnpm typecheck` 通过；`tests/unit/reading.test.ts` 与 `tests/unit/ai-gateway.test.ts` 共 17 项测试通过；15 种摘要选项组合通过 API schema 和两种 provider 请求构造校验。相关文件 Prettier 通过，浏览器异常列表为空。
- worker 更改后已在无排队／处理中任务时正常停止并重启，确认 ready。未修改上传或任务状态机。
- 浏览器验证使用已保存的真实 PDF 预览、只读签名链接及内存中的摘要／导图样例。生成请求在测试浏览器内被拦截，无本轮测试触发的真实 AI 生成或费用；未验证新增模板的真实模型输出质量，也未修改原翻译任务的中断状态。

没有待处理的 P0/P1/P2 布局或交互问题。未执行提交、push、PR、远程 CI 或部署。

final result: passed

## 2026-09-16：Pricing 四档美元展示

- source visual truth path: `/var/folders/ns/ds37t0716qd6_y1m1bswswwm0000gn/T/codex-clipboard-c0e394da-e9ca-4908-b19b-edf7082e522c.png`。
- 用户确认采用 $0 / $9 / $29 / $59。参考范围为套餐卡片和打勾权益列表；保留 FormatOwl 导航、一次性积分包、现有积分数与销售关闭状态。免费档是示例／工具入口，不赠送积分，不复制参考站的包月、上传容量或优先处理承诺。
- implementation screenshot path: `.data/pricing-qa/desktop-complete.png`；归一图 `.data/pricing-qa/desktop-normalized.png`；同输入并排卡片对照 `.data/pricing-qa/cards-comparison.png`，已打开检查。
- 参考原图 2940 × 1666，按 2 倍归一；实现 CSS viewport 1470 × 1200，devicePixelRatio 0.9。应用内截图提供方输出 1633 × 1333，实际内容位于左上半宽／半高区域；归一时截取 816 × 666 再恢复为 CSS 尺寸。图像采样模糊不视为页面字体问题。
- 对照状态：中文定价页面、余额已加载、无账单、购买关闭。参考含浏览器侧栏且页面已滚动，页首并非同一导航／滚动状态，因此用四张卡片内容区域进行布局比较。整页层级由桌面归一截图检查；卡片对照已覆盖价格、标题、按钮与所有权益，无需更小的局部截图。

### 检查与修正

- 字体：沿用项目 Arial / PingFang SC，套餐标题 26px、价格 36–46px、权益 14px。标题、按钮、价格与权益分层明确，无截断。
- 间距：桌面四等宽卡片，24px 间距、26px 圆角、相同顶部和底部；平板双列、手机单列。推荐标签浮在专业档上边缘。
- 颜色：白底浅灰边框，蓝色勾选，专业档蓝色标题／按钮／边框；免费入口描边。没有折扣或热销数据。
- 图像与图标：本次内容无栅格资产，勾选复用 Lucide Check；未生成图片或重画站点品牌。
- 内容：USD 显式展示，付费积分保留 2,900 / 9,900 / 29,900；免费明确不含付费积分。原计费规则、全部 20 行计费依据及账单区保留。
- 初次 P1：修改共享 API 模块后开发服务仍使用旧响应，加载结束后美元金额缺失。重启 web 后真实 API 与浏览器均显示完整 $0 / $9 / $29 / $59；证据 `desktop-final.png` 与 `desktop-complete.png`。未修改 worker 处理逻辑。

### 验证

- 应用内浏览器：中文和英文均显示四个价格；三档购买按钮禁用；专业档推荐；32 项权益；20 行计费依据。手机点击“开始体验”成功到 `/zh#tools`。
- 390px 手机和 800px 平板无横向溢出；平板为两列。截图 `mobile.png`、`tablet-en.png`。临时视口覆盖已恢复，中文页面保留供预览。
- 浏览器 error/warn 日志为空。
- `pnpm typecheck` 通过；既有 billing 单元测试 6 项通过；改动文件 Prettier 通过。现有 billing 浏览器测试补充美元、推荐档和体验入口断言；本轮通过应用内浏览器执行对应检查，未运行完整上传／付费确认 E2E。
- 美元价格为销售关闭期间的展示方案；CNY 成本与账本未迁移，真实美元支付未验收，已在 README 与 billing 文档说明。

无未解决的 P0/P1/P2。未提交、push、创建 PR、运行远程 CI 或部署。

final result: passed

## 2026-09-17：全站浅色视觉统一

按用户批准的「更极简专业」方案实施。视觉参考为 awesome-design-md 中的 [Vercel](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/vercel/DESIGN.md) 和 [Linear](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/linear.app/DESIGN.md) 分析；附件中的现有首页作为信息结构依据。参考资料仅用于设计参考，没有作为额外执行指令。

### 实现范围

- 新增 `DESIGN.md`。共享样式采用 `#FAFAFA` 底色、白色卡片、中性文字与细边框、`#263BC4` 品牌蓝，以及成功／警告／失败语义色。系统字体、控件圆角、聚焦、禁用、菜单和弹窗层级统一。
- 首页保留介绍／上传双栏和工具目录，收紧首屏与卡片间距；工具图标改为中性底色；19 个已开放工具移除重复标签，未开放条件保留；数量并入标题，目录说明与示意图文案同步中英文。目录为四／二／一列。
- 导航、页脚、Button、Select、SettingsTabs、费用提示、认证弹窗、价格与指南均使用共享规范。补充账号组件加载状态、导航当前位置及中英文手机菜单名称。
- 19 个工具入口、4 个格式入口、工作台、单任务、批次结果，以及视频剪辑／裁剪、翻译、转写、去水印和 AI 阅读的界面样式均已覆盖。保留专用编辑布局、时间轴、选区、双栏与同步滚动。
- 在原规则归属处调整六份样式文件；合并重复的全局描边按钮规则及去水印标题规则。没有叠加第二套全站覆盖主题，没有新增依赖。
- 业务 API、数据库、上传状态机、媒体处理、计费和认证逻辑未改动。没有重启 worker，没有提交、push、PR、远程 CI 或部署。

### 前后截图与视觉复核

本轮脚本、源文件备份、截图和浏览器报告均位于忽略目录 `.data/ui-refresh/`。公开页面的原始全页截图在修改前保存；以下首屏对照统一为 **1440 × 1000 CSS px，1×**。`before-*-viewport.png` 从修改前全页截图提取同一首屏区域，没有缩放。原始完整截图仍保留为 `before-home.png` 等；新版完整页面由于收紧间距，高度有所改变。

| 页面         | 修改前                         | 修改后                        |
| ------------ | ------------------------------ | ----------------------------- |
| 首页         | `before-home-viewport.png`     | `after-home-viewport.png`     |
| 视频压缩设置 | `before-settings-viewport.png` | `after-settings-viewport.png` |
| 价格         | `before-pricing-viewport.png`  | `after-pricing-viewport.png`  |
| 登录         | `before-login-viewport.png`    | `after-login-viewport.png`    |
| 单任务结果   | `before-result-replayed.png`   | `after-result.png`            |
| PDF 阅读器   | `before-reader-replayed.png`   | `after-reader.png`            |

结果页与阅读器的前图使用相同测试数据、当前组件结构及备份 CSS 回放，因此是**原样式回放基线**，不是历史完整版本构建。两组均为 1440 × 1000。页面状态分别为压缩完成、仅译文加 AI 摘要；阅读器另保存双语、导图、导出弹窗和手机截图。

已打开实际截图检查字体、间距、对齐、颜色、图标和文字层级。桌面首屏仍能直接识别产品用途和上传动作；工具卡片边框、格式行和图标对齐；设置／结果／价格／登录页保持相同控件语言。阅读器保留文档内容区域，标题、页码和下拉框不再使用装饰性的蓝色。

手机截图包括 `after-home-zh-390.png`、`after-settings-zh-390.png`、`after-login-zh-390.png`、`after-register-mobile.png`、`after-reader-mobile-ai.png`、`after-reader-touch-320.png`、`after-watermark-touch-320.png`，以及工作台／批次长文件名状态截图。真实剪辑与转写截图沿用测试输出 `.data/video-cutter-design-mobile.png`、`.data/video-crop-design-390.png`、`.data/transcript-editor-accepted.png`。

### 发现、修正与复验

| 问题                                                    | 修正与结果                                                                                           |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| 320px 公共导航品牌与操作入口挤出页面                    | 收紧手机导航间距，调整最窄断点的品牌尺寸；中英文全部路线复验无横向溢出。                             |
| 阅读器手机选中标签前景对比不足                          | 统一浅蓝选中底与蓝色文字；390px 和 320px 触屏复验通过。                                              |
| 旧价格区域中的 `.button-outline` 影响全站按钮           | 合并到共享规则，次级按钮恢复中性前景和白色背景。                                                     |
| 去水印工作区 320px 长标题与操作按钮相挤                 | 标题容器允许缩小，手机操作行和工具栏按空间换行；含 44px 触屏控件、画笔滑杆的复验通过。               |
| 真实裁剪测试上传分片返回 404                            | 追踪确认上传与分片请求间会话 cookie 改变；测试补充已有会话就绪等待后通过。未更改上传或认证业务实现。 |
| 转写测试的通用 `role=status` 同时命中费用提示和保存状态 | 将保存断言限定到转写文件状态区；编辑、冲突保护和五种导出全部通过。                                   |

### 验证结果

- **320 次公开路线布局检查通过**：32 条路线 × 中英文 × 1440／1024／768／390／320px，包含 19 个工具、4 个格式入口、首页、工作台、价格、登录、重置密码、指南及三篇文章。报告 `route-audit.json`：0 横向溢出、0 未捕获页面异常。
- **76 项结果／状态／去水印检查通过**：长文件名、完成／处理／失败／过期状态、工作台和批次操作换行、加载／空白／失败提示，以及去水印选择、撤销与窄屏画笔；报告 `states-audit.json`。此组用浏览器固定响应，不改真实任务。
- **18 项阅读器检查通过**：等宽分栏、双语同步滚动、摘要菜单、导图缩放／适应、校正开关、下载弹窗、两种语言五种宽度及 390／320px 触屏 AI 面板；报告 `reader-audit.json`。
- **29 个不同的现有 Playwright 用例最终通过**，分组执行：设置标签 3、下拉控件 2、SEO 6、费用提示 4、认证 2、价格 1、首页上传／手机导航 2、视频剪辑与裁剪 2、格式转换／转写 6、去水印入口 1。重复执行未重复计数。
- 真实本地流程包括：首页上传 → 视频压缩 → 刷新 → 下载；视频／音频／图片转换 → 预览 → 下载 → ZIP；剪辑播放／选区／触摸手柄；裁剪比例／缩放边缘／定位／音量／格式草稿；转写编辑、分页、说话人、冲突保护及 TXT／DOCX／PDF／SRT／VTT 导出。
- 本地邮件模式的注册、验证、登录、退出、密码重置及认证弹窗回归通过。邮件仅写本机测试目录，未向外部邮箱发送。
- `pnpm typecheck` 通过；`pnpm build` 通过；本轮改动文件 Prettier 检查通过。纯样式未新增模拟实现的单元测试。

### 本轮没有验收的内容

- 阅读器使用已有 22 页任务结构、合成摘要／导图及本地 PDF 测试页图检查网页界面；所有相关生成请求在浏览器内拦截。未发起真实翻译、OCR、摘要或问答生成，也没有产生本轮 AI 生成费用；不以这些截图证明文档翻译或排版质量。
- 转写用预置的 105 段文本验证真实编辑、保存和导出；没有重新调用语音识别。去水印工作区的绘图检查使用固定结果，未重新验证修复模型质量。
- 未穷举每种媒体格式和参数组合、Safari／Firefox、实际第三方 OAuth、远程邮件送达或线上支付。这些不作为本轮纯视觉验收的通过项。

本轮界面范围内没有已知待修复的布局阻断问题。后续页面以 `DESIGN.md` 为统一依据。

## 2026-09-17 — 暖白视觉重构与公开示例体验

### 实现范围

- 以 Intercom 的暖白、深色文字和产品内容展示为主要方向；品牌蓝用于主要操作、选中和链接。更新 `DESIGN.md`，统一共享颜色、16px 内容卡片、8px 控件、48px 主要按钮与弹窗。
- 首页保留介绍／上传双栏与 19 个工具，重做四个格式快捷入口、三篇指南卡片、三步说明和四项 FAQ。移除旧的示意性压缩宣传图，增加三个真实公开示例。首页资源链接继续服务端渲染。
- 工具说明页与指南复用资源卡片；共享变量覆盖工作台、结果、认证、价格、翻译／转写／阅读／去水印工作区。将原本只在翻译样式中的通用弹窗规则移至全局归属，移除阅读器残留的独立亮蓝。
- 价格页“开始体验”指向 `#examples`，可操作按钮与未开放充值按钮有明确区别。导航、页脚、中英文文案与手机布局同步调整。
- 新增本地类型化样例清单与可选 `initialSampleId` 输入。只接收清单中匹配当前工具的样例标识；不接受任意 URL。不新增 API、数据库结构或计费分支。
- 样例预览／下载是静态资源；加载样例只选择文件与预设，实际上传和处理由原有按钮触发。已有选择须明确确认替换；失败可重试，取消会中止加载，迟到的响应不会覆盖更新的选择。

### 样例来源与测量

素材由 `scripts/public-examples.ts` 自行绘制，通过现有 worker 处理器离线生成；使用现有 FFmpeg、Sharp、Python／PDFium 依赖，没有引入新的运行时依赖。`pnpm fixtures:public` 已实际重跑成功。公开素材共约 5 MiB，不包含用户上传内容；主页视频 `preload=none` 且不自动播放，图片和 PDF 缩略图按需加载。

| 样例            | 原始文件（字节） | 结果（字节） | 实际设置                |
| --------------- | ---------------: | -----------: | ----------------------- |
| 8 秒原创视频    |        1,628,966 |      160,705 | H.264、均衡、原始分辨率 |
| 透明 WebP → JPG |           12,732 |       23,113 | 质量 85、白色背景       |
| 单页 PDF        |        2,799,627 |       50,412 | 强力压缩                |

图片转换后的体积增长如实展示；所有数值只代表该样例。PDF 网页预览为对应文件渲染的缩略图，原件与结果均可下载检查。

### 截图与布局证据

本轮证据保存在被 Git 忽略的 `.data/visual-rebuild/`。

- 首页、设置、结果、阅读器、登录、价格 **6 组 1440 × 1000 同尺寸前后截图**：`before/{name}.png` 与 `after-{name}.png`。
- 格式入口、样例和指南细节：`after-shortcuts.png`、`after-examples.png`、`after-guides.png`。
- 中英文桌面和手机整页截图、工作台／批次状态、去水印及阅读器触屏截图也保存在同目录。
- **320 项公开路由布局检查通过**：32 条路线 × 中英文 × 1440／1024／768／390／320px，覆盖全部 19 个工具、4 个格式入口及公共页面；`route-audit.json` 记录 0 横向溢出、0 未捕获脚本异常。
- **76 项结果／状态／去水印检查通过**：长文件名、完成／处理中／失败／过期、批次操作、工作台加载／空白／失败及选区／撤销／手机画笔，见 `states-audit.json`。
- **18 项阅读器检查通过**：等宽分栏、双语同步滚动、摘要菜单、导图缩放、校正、导出弹窗、两种语言五种宽度和触屏面板，见 `reader-audit.json`。

### 浏览器与工程验证

- 原有页面／功能回归组最终 **25 项通过**：导航、工具入口、设置与下拉框、认证弹窗、价格与费用提示、SEO、首页真实上传下载及转写编辑／导出。证据：`regression.log`、`regression-retry.log`。
- 新增样例流程 **10 项通过**：中英文免费预览及真实文件下载、三个来源与预设、非法／不匹配标识、明确替换、失败重试、竞态保护、窄屏弹窗和真实提交至下载。最终与认证／价格一起执行的 `final-smoke.log` 为 **12/12 通过**，重复用例不重复计数。
- 真实编辑器 **7 项最终通过**：音轨提取及 MP3 下载、视频剪切及 MKV 导出、精确裁剪及 MOV 尺寸验证、双来源音频拼接／淡入淡出／M4R、手机触摸裁剪与草稿、剪辑触摸手柄、裁剪比例／定位／音量／格式保持。证据：`editors-final.log`（4 项）、`crop-final.log`（1 项）与 `touch-final.log`（3 项，其中 1 项重复）。本轮共 **42 个不同的浏览器测试用例最终通过**。
- 类型检查、生产构建及改动文件 Prettier 检查通过：`typecheck-final.log`、`build-final.log`、`format-final.log`。
- 初轮修正了样例测试中的预设／下载按钮定位，以及新增页脚后导航测试的定位范围。两项在并发构建时超时的回归用例，单独重跑通过。
- 编辑器旧测试存在先打开页面再初始化匿名会话的竞态：上传创建成功，但分片请求因会话变化返回 404。测试现在在导航前建立匿名会话，再等待界面就绪；此项属于测试准备调整，没有修改上传或认证业务实现。

### 验收边界

- 阅读器使用已有任务结构和固定的摘要／导图／页图响应；相关生成调用在浏览器中拦截。转写使用预置文本验证真实编辑和导出，没有重新调用语音识别。本轮没有付费 AI 生成，不以视觉截图证明翻译、识别或修复质量。
- 真实处理在本机现有试算模式下验证；没有真实扣费、购买或支付验收。示例继续处理仍受部署环境的现有认证、计费和开放状态控制。
- 未重新验收第三方 OAuth、远程邮件送达、Safari／Firefox及全部媒体格式组合。没有 push、PR、远程 CI 或部署。

## FormatOwl 品牌更新（2026-09-17）

- 中英文导航、页脚、登录／重置密码、积分、帮助文章、工具说明、页面标题、SEO、邮件和 AI 阅读身份已统一为 FormatOwl。新导出文件及 ZIP 使用 `formatowl` 文件名。
- 重制蓝底猫头鹰 SVG，并同步社交分享图、16／32／48px favicon 与 180px Apple 图标。导航、登录与翻译编辑器共用 SVG 入口。
- 重新生成公开示例视频、海报、PDF 和预览；核对 PDF 文字及文件大小清单，未处理用户上传文件。
- 保留内部包名、数据库、存储、队列、Cookie 和草稿键，避免品牌改名导致原服务、登录和草稿失效；历史迁移保持不变。
- 验证：类型检查、生产构建、95 项单元测试、6 项 SEO 浏览器测试通过；注册验证／密码重置／游客接续／移动登录流程通过；真实视频导出下载、图片转换与 ZIP 下载通过。网页与 worker 已重启。
- 视觉检查：首页桌面和移动端、英文移动登录、分享卡片及 PDF 预览；截图保存在忽略目录 `.data/formatowl/`。
- 英文首页 1440／1024／768／390／320px 均无横向溢出或旧品牌文字；320px 登录页同样通过。favicon 与 Apple 图标请求均返回 200。
- 认证安全用例首次受前序注册操作的 60 秒限流影响；等待窗口结束后单独复验通过。一次并发测试产生了报告目录竞争，最终复验使用独立输出目录且串行运行。

## FormatOwl 图标重制（2026-09-17）

- 根据用户反馈，联网查看 Dribbble 与 Behance 的几何猫头鹰标志参考，使用内置 ImageGen 重新生成完整猫头鹰造型，翅膀融入纸张折角；来源与两轮提示词记录于 `assets/brand/README.md`。
- 第二轮清理不均匀透明背景，最终使用不透明蓝底，网页及分享图以圆角展示。原图保存在 `assets/brand/formatowl-master.png`，网站入口改为 256px PNG；ICO 为 16／32／48px RGBA PNG 容器，Apple 图标为 180px。
- 已同步导航、页脚、登录、翻译编辑器、favicon、Apple 图标与分享图，并移除旧 SVG。检查桌面导航、390px 英文登录及 1200×630 分享图，图标加载正常、无横向溢出；favicon 返回 200。
- 最终生产构建（含 TypeScript）与分享图浏览器用例通过。
